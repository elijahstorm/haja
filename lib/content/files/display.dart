import 'package:flutter/material.dart';

import 'content.dart';

class FileContentDisplayPage extends StatelessWidget {
  final FileContent content;

  const FileContentDisplayPage(
    this.content, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [],
      ),
    );
  }
}
