import 'package:flutter/material.dart';

import 'content.dart';

class AdvertisementContentDisplayPage extends StatelessWidget {
  final AdvertisementContent content;

  const AdvertisementContentDisplayPage(
    this.content, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [],
      ),
    );
  }
}
